<Window x:Class="Quizzify.Client.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resource="clr-namespace:Quizzify.Client.Resources.Locales"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="5">
            <TextBlock Text="Введите IP:" />
            <TextBox Text="{Binding Ip}"/>
            <TextBlock Text="Введите порт:" />
            <TextBox Text="{Binding Port}"/>
            <Button Content="Подключиться" Command="{Binding ConnectToGameCommand}"/>
        </StackPanel>
    </Grid>
</Window>